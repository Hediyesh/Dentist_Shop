@model DataLayer.ProductGroup

@{
    ViewBag.Title = "Edit";
    Layout = null;
}

@using (Ajax.BeginForm("Edit", "ProductGroups", FormMethod.Post, new AjaxOptions()
{
    OnSuccess = "hideGrModal",
    UpdateTargetId = "listGroup"
}))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.parentId)
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.groupId)
    <div class="row">
        <div class="col-lg-1 col-md-1 col-sm-1 col-xs-1"></div>
        <div class="col-lg-3 col-md-3 col-sm-3 col-xs-3">
            @Html.LabelFor(model => model.groupName, htmlAttributes: new { @class = "control-label" })
        </div>
        <div class="col-lg-7 col-md-7 col-sm-7 col-xs-7">
            @Html.EditorFor(model => model.groupName, new { htmlAttributes = new { @class = "form-control", @style = "width:100%" } })
            @Html.ValidationMessageFor(model => model.groupName, "", new { @class = "text-danger", @id = "groupNameEditError" })
        </div>
        <div class="col-lg-1 col-md-1 col-sm-1 col-xs-1"></div>

    </div>
    <br />
    <div class="row ">
        <div class="col-lg-1 col-md-1 col-sm-1 col-xs-1"></div>
        <div class="col-lg-10 col-md-10 col-sm-10 col-xs-10">
            <input type="submit" value="ویرایش گروه" class="btn btn-sm btn-warning" style="float:right" />
        </div>
        <div class="col-lg-1 col-md-1 col-sm-1 col-xs-1"></div>
    </div>

}
